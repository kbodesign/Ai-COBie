namespace OmniClass.Revit.Ribbon
{
    /// <summary>
    /// Kept free of Revit types so the placement can be asserted in tests that do not
    /// load the Revit API.
    /// </summary>
    public static class RibbonNames
    {
        public const string Tab = "Arch Tools";
        public const string Panel = "Room Data";
    }
}
